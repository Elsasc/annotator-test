in /frontend
Make sure to install the 
npm install --save gh-pages


To deploy the GH pages
npm run deploy 


in /backend

Make sure to install 
pip install Flask flask-cors

pip install pymupdf

To run backend, cd into /backend and 
python app.py 